# Filter Sublist Using Lambda

## Problem
Write a Python program to filter a list of integers using **lambda**.

You must separate the list into:
- Even numbers
- Odd numbers

Use **lambda with the `filter()` function**.

---

## Input
A list of integers.

Example:
